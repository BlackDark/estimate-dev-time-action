{
  "name": "estimate-dev-time-action",
  "version": "1.0.0",
  "description": "GitHub Action to estimate development time for PRs using AI",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "pnpm run clean && tsc && esbuild dist/index.js --bundle --platform=node --target=node20 --format=cjs --outfile=dist/main.js --minify --sourcemap && mv dist/main.js dist/index.js && pnpm run clean:artifacts",
    "package": "pnpm run build",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts",
    "clean": "rimraf dist",
    "clean:artifacts": "rimraf dist/*.d.ts dist/*.d.ts.map dist/*.js.map dist/formatter.js dist/github.js dist/openrouter.js dist/types.js && rimraf dist/main.js.map"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-username/estimate-dev-time-action.git"
  },
  "keywords": [
    "github-action",
    "pr-estimation",
    "development-time",
    "ai"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "openai": "^5.16.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.3.0",
    "@typescript-eslint/eslint-plugin": "^8.41.0",
    "@typescript-eslint/parser": "^8.41.0",
    "esbuild": "^0.25.9",
    "eslint": "^9.34.0",
    "jest": "^30.1.0",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.41.0"
  }
}
